import React from "react";

export default function ProjectList() {
    return (
        <div className="project-list board-container">
            <p>Keep working on projects:</p>
        </div>
    );
}